const num1 = Number(prompt('Введите число 1:'));
const num2 = Number(prompt('Введите число 2:'));

const matrix = [];

for(let i = 0; i <= num1; i++) {
    matrix.length = num1 - 1;

    for(let j = 0; j <= num2; j++) {

    }    
}